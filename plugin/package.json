{
  "name": "@ai-driven-engineering/opencode-plugin",
  "version": "6.1.3",
  "private": true,
  "type": "module",
  "exports": "./index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --test --test-concurrency=1 tests/*.test.mjs"
  },
  "description": "OpenCode V2-native runtime for AI-Driven Engineering, aligned to the official Promise plugin contract",
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.18.15"
  }
}
